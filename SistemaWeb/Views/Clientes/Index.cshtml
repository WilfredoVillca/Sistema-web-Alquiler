@model IEnumerable<SistemaWeb.Models.Cliente>

@{
    ViewData["Title"] = "Index";
}

<h1 style="text-align:center;font-weight:700;color:aliceblue;margin-top:18px; font-family:'Times New Roman';"> Lista de Clientes</h1>

<p class="parrafo"> 
    <a asp-action="Create" style="color:white;">Registrar Cliente</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ci)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefono)
            </th>
            <th> Opciones</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ci)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="editar">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="editar">Detalles</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="editar">Eliminar</a>
            </td>
        </tr>
}

        <style>
            .editar {
                border: 7px solid forestgreen;
                border-radius: 8px;
                width: 100px;
                height: 40px;
                background-color: forestgreen;
                color: white;
            }


            .parrafo {
                width: 180px;
                height: 40px;
                border-radius: 15px;
                text-align: center;
                margin: 0 40px;
                border: solid 2.5px #ffff;
                background-color: #2a802c;
            }
                .table {
                background-position: center;
               
                text-align: center;
                margin-top: 50px;
                backdrop-filter: contrast(0.15);
            }
        </style>
    </tbody>
</table>
